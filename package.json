{
  "name": "shared-component",
  "description": "Share a component instance inside a window context",
  "repository": "yoshuawuyts/shared-component",
  "version": "1.0.1",
  "scripts": {
    "deps": "dependency-check . && dependency-check . --extra --no-dev",
    "start": "node .",
    "test": "standard && npm run deps"
  },
  "dependencies": {},
  "devDependencies": {
    "dependency-check": "^2.9.1",
    "standard": "^10.0.2",
    "tape": "^4.7.0"
  },
  "keywords": [
    "share",
    "component",
    "window"
  ],
  "license": "MIT"
}
